// Main entry point for the FocusFlow dashboard.
// This file stays intentionally thin and delegates to the AppShell.

import { AppShell } from "@/components/dashboard/app-shell";

export default function HomePage() {
  return <AppShell />;
}

